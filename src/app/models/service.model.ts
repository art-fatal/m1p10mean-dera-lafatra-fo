export class ServiceModel {
    constructor(
        public id: string,
        public name: string,
        public duration: string,
        public price: number,
        public commission: number,
    ) {
    }
}
